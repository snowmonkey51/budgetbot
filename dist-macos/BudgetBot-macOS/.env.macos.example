# BudgetBot macOS Configuration Template
# Copy this file to .env and customize as needed

# Application Environment
NODE_ENV=production
PORT=5000

# Database Configuration
# Option 1: In-Memory Storage (Default - no database required)
# Data will reset when application restarts
# Perfect for quick testing and personal use

# Option 2: PostgreSQL Database (Persistent storage)
# Uncomment the line below and configure your PostgreSQL connection
# DATABASE_URL=postgresql://username:password@localhost:5432/budgetbot

# Optional: Custom Configuration
# CUSTOM_PORT=3000
# CUSTOM_HOST=127.0.0.1

# Security (for production deployments)
# SESSION_SECRET=your-random-secret-key-here

# Logging
# LOG_LEVEL=info