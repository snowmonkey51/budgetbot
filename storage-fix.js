// Quick fix for BudgetBot macOS - Copy this content to server/storage.ts at the end
// Replace the last line: export const storage = new DatabaseStorage();
// With: export const storage = new MemStorage();